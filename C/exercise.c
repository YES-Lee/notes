#include <stdio.h>
#include <stdlib.h>

#include "mymath.h"

// #define SUM(a,b) ((a) + (b))
// #define PRT(s) printf(""#s": %d\n", s)

int main() {
  // PRT(SUM(1,2));
   printf("%d\n", sum(1,2));
  return 1;
}
